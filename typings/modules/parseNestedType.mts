// Todo: move some of generate-typings stuff here
// All enums must be hoisted to highest scope, nested enums arent a thing in ts
// name of hoisted would be combination of parent name and nested name

export function parseNestedType(type: unknown): string {
	
} 
{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Current file",
			"program": "${file}",
			"request": "launch",
			"type": "bun",
			"cwd": "${workspaceFolder}"
		},
		{
			"name": "Generate Types",
			"program": "${workspaceFolder}/generate-types/generate-typings.mts",
			"request": "launch",
			"type": "bun",
			"cwd": "${workspaceFolder}"
		}
	]
}